#+TITLE: 🪐 Offworld
#+OPTIONS: toc:2 num:nil

* 🪐 Offworld

Offworld runs one Clojure expression in three places: rendered on a server and
morphed into the browser, rendered entirely in the browser, or — when the network
drops — rendered in the browser from a snapshot the server left behind. Same
render-fns, same handlers, no port. You mark which handlers need the server, and
Offworld splits each interaction at dispatch time.

Three libraries do the heavy lifting:

- *Replicant* — renders Clojure data structures to DOM, and hands you one hook
  where every interaction arrives, plus hooks for elements appearing and leaving.
  No component model.
- *Nexus* — a table of named handlers, and a loop that drains a list of things to
  do by looking each one up as it goes.
- *Datastar* — attribute-driven client behaviour, and DOM /morphing/ over SSE:
  patching a live DOM tree in place to match a new HTML fragment, pushed down a
  long-lived HTTP response the server keeps writing to.

Offworld adds one idea, and the rest of the library follows from it:

#+begin_quote
An interaction is a /staged computation/. Represent it as data, and mark each
handler with the stage it runs at.
#+end_quote

/Staged/ because the parts of one interaction can't all run at the same moment.
What the season is now, the server knows at render time; which modifier key you
were holding, only the browser knows, and only once you've clicked. A staged
computation is one value whose parts are evaluated at different points along that
chain, rather than code that runs all at once.

So: no component model, no reactive graph, no signals, no compiler inferring where
your code runs. A vector of intents, a registry of handlers, and one piece of
metadata — =^::🪐/server= or =^::🪐/client= — saying where a handler lives. Terms
that might be unfamiliar are collected in the [[*Glossary][Glossary]] at the end.

** Quickstart

#+begin_src sh
bb dev            # shadow-cljs watch + http-kit on :8000 + clerk on :9001
bb test-clj       # jvm tests
bb test-cljs      # node tests via shadow
#+end_src

- [[http://localhost:8000][localhost:8000]] — server-rendered. The browser holds no state; the server renders
  hiccup to HTML and morphs it down an SSE connection.
- [[http://localhost:8000?csr][localhost:8000?csr]] — the same UI, entirely client-side. Replicant reconciles a
  virtual DOM; no server after the first byte.
- Kill the server and the SSR page keeps working. See [[*Offline][Offline]].

The library is seven files under a thousand lines: =offworld.cljc=, =stem.cljc=,
=staging.cljc=, =order.cljc=, =inline.cljc=, =util.cljc=, =nexus/registry.cljc=.
Everything under =demo/= is the demo.

* Why

** Two paradigms, one program

Server rendering is appealing — one place for state, one language, no cache to
invalidate — but it always needs a server, and the connection drops. Rich clients
are appealing for the opposite reason, resilient and immediate, but you end up
rebuilding in the browser what the server already had: a store, a query layer, a
migration story. Most codebases pick one and live with it, and switching or
combining means a rewrite. We're using Clojure, though. Simplify the fundamental
patterns far enough and the flexibility might come free.

** The stages are real whether or not you name them

Any interactive server-rendered app has a fixed pipeline, and it's physically
real: the DOM event only exists in the browser, the database only on the server,
and something has to carry a value from one to the other.

| stage           | inputs             | action       | outputs           |
|-----------------+--------------------+--------------+-------------------|
| Morph           | ↪SSE message   --> | morph        | --> new DOM       |
|-----------------+--------------------+--------------+-------------------|
| Client Dispatch | actions        --> | ↪interpolate |                   |
|                 | client-event   --> | expand       |                   |
|                 | client-state   --> | recur↩       |                   |
|                 |                    | interpolate  |                   |
|                 |                    | effect       | --> client-fx     |
|-----------------+--------------------+--------------+-------------------|
| HTTP Request    | server-actions --> | serialize    | --> dispatch RPC  |
|-----------------+--------------------+--------------+-------------------|
| Server Dispatch | server-state   --> | ↪interpolate |                   |
|                 |                    | expand       |                   |
|                 |                    | recur↩       |                   |
|                 |                    | effect       | --> server-fx     |
|-----------------+--------------------+--------------+-------------------|
| Render          | server-state   --> | render       | --> new actions   |
|                 |                    |              | --> SSE-message↩  |

Every hybrid framework has this pipeline. What differs is whether the stages are
written down anywhere, or reconstructed in your head each time.

The common approach leaves them implicit and scattered: a fragment of JavaScript
in a string attribute reads =evt.key= (client stage); an opaque server-side
closure holds the business logic (server stage); a payload key matched by string
convention ties the two together. Three authoring sites, three languages, nothing
that can see all of them at once.

Offworld's bet is that one data structure carrying all the stages is worth more
than the directness you give up.

** A function can't cross the wire

Start from what you'd write if nothing were in the way — a button that changes the
season:

#+begin_src clojure
[:button {:on {:click (fn [] (set-season! :winter))}} "Winter"]
#+end_src

That handler is a *closure*: a function bundled together with the bindings it
captured from where it was written. In a browser-only app this is fine, because
the function is created and called in the same place.

Now render that same button on the server. The click will happen in a browser,
later, on a different machine, and the only thing a server ever hands over is
text — HTML, and whatever the HTML carries. A closure has no written form. You can
print its source, but the bindings it captured (a database connection, the current
season, the user) were never text and can't become text.

So you do the only thing available: leave the function where it can run, give it a
name, and send the name along with the values it needs.

#+begin_src clojure
[:button {:on {:click [[:set-season :winter]]}} "Winter"]
#+end_src

The browser sends that vector back. The server looks =:set-season= up in a table,
finds the function, applies it to =:winter=. Nothing crossed the wire but a
keyword and a value.

That move has a name — *defunctionalization* — and it's worth knowing, because
once you see it you see it everywhere. Reynolds described it in 1972 as a compiler
technique: replace a function value with a tag plus the values it captured, and
have one dispatcher apply the tag to them. That's precisely what the two snippets
above do.

The reason to raise it here is that it isn't a style choice you can decline. Any
framework that runs one interaction across two machines defunctionalizes, because
a closure can't be serialized. The only real question is what it defunctionalizes
*into*:

- into *strings* — an event name, a payload key, a JavaScript expression pasted
  into an attribute
- into *places* — a named cell in the markup whose meaning lives in a callback
  registered somewhere else
- into *macros* — a compiler that infers which side each expression belongs to
- into *data* — a Clojure vector

Offworld picks data. Not because data is virtuous, but because of what a value
admits that the other three don't:

- *It crosses.* A closure can't be split across a wire. Every action Nexus reaches
  is classified by the side tag on its head, at any expansion depth. Without this
  there's no hybrid mode at all; the rest of the list is a bonus.
- *It can be substituted into.* Interpolation walks the tree and replaces what it
  finds, at every action node, on both sides. That's what "you can =postwalk= it"
  cashes out to — the mechanism every dispatch depends on.
- *It can be checked before it runs.* Stranded client references and unregistered
  heads fall out of walking the same structure, statically, without executing
  anything.
- *It can be shown.* Every key classified by side and kind, coloured in the editor,
  read out on hover. An intent prints at its call site with =#p=.
- *It can be extended by a caller who doesn't understand it.* A layer that knows
  nothing about a component's concern can still =into= or =mapv= the intent it was
  handed, so components compose without either layer naming the other's business.
- *It can be recorded and re-run.* Offline mode builds a log while the connection
  is down; reconnecting replays it rather than diffing two states.
- *A change to it reads as a change* — an altered vector in a diff, not an altered
  string of JavaScript.

Two more are reachable and unbuilt: authorizing a dispatch by its /shape/ before
anything runs, and compiling a statically-known dispatch to direct client code.

None of this comes free, and the fair objection is easy to state: a table of names
you look functions up in is an interpreter, and writing one is usually a mistake.
That's taken up under [[*Objections][Objections]]. The short reply is that the interpreter is there
either way. Either it's Nexus — written down, inspectable, the same on both sides
— or it's an unwritten convention in a colleague's head.


* The loop, walked

One real interaction from the demo, end to end. A button in the holiday panel,
where shift-clicking means something different from clicking.

#+begin_src clojure
(defn randomize-button [{::🌿/keys [stem]}]
  [:button {:on {:click [[::randomize
                          [:event/key-modifiers]
                          (get-season stem)]]}}
   "Randomize (shift-click to reset)"])
#+end_src

Two of that vector's three elements are at different stages, and the notation
shows which is which.

** Render — on the server

=(get-season stem)= is an ordinary function call. It runs /now/, and what lands in
the vector is the value =:spring=. =[:event/key-modifiers]= is not a call. It's a
vector and it stays one, naming a value that doesn't exist yet because the click
hasn't happened.

That's the whole notation. Anything you can compute at render time you compute
with Clojure. Anything you can't, you /name/, and the name travels.

Then =🪐/replicant->d*= walks the hiccup and turns the =:on= map into a Datastar
attribute:

#+begin_src html
data-on:click="((_sp)=>_sp&&@get('/offworld-dispatch',{payload:{offworld:_sp}}))
               (nextjournal.offworld.divert('<transit-base64>', evt))"
#+end_src

There /is/ string interpolation here, but as dumb transport rather than code
generation: one opaque blob in one fixed slot, beside a dispatch URL constant for
the whole app. The JavaScript around it is byte-identical on every element, and no
part of what an action /means/ is ever assembled into it. Not "no interpolation"
but *one* interpolation — as against the common pattern where each seam
hand-writes its own attribute expression, so every interaction invents a little
language only the browser can read.

** In the browser

The click fires. =divert= decodes the payload and dispatches it against a /client
view/ of the registry — one whose placeholders are filtered to the client-marked
ones. Each action is then /interpolated/ — walked, with every placeholder
replaced by the value it stands for — as Nexus reaches it, so
=[:event/key-modifiers]= resolves against the real DOM event and
=[[::randomize [:shift nil nil] :spring]]= is what expands.

=::randomize= is client-marked, so it expands here:

#+begin_src clojure
(nxr/register-action! ::randomize ^::🪐/client
  (fn [_ key-mods season]
    (let [path        [::path :to :season]
          reset?      (contains? (into #{} key-mods) :shift)
          rand-season (first (rand-nth (seq (dissoc season->holiday season))))]
      (if reset?
        [[:browser/alert "Holiday season has been reset."]
         [:effects/save path :spring]]
        [[:effects/save path rand-season]]))))
#+end_src

The shift-key branch is decided in the browser with no round trip — an ordinary
=if=, in ordinary Clojure, because by this stage the value it tests is a value.

The two effects it returns don't live in the same place. =:browser/alert= is
client-marked and runs immediately. =:effects/save= is =^::🪐/server=, so an
interceptor at Nexus's =:before-action= phase recognises the tag, sets the action
aside for the server, and empties the rest of that action's queue so nothing runs
it locally.

That phase is why the split is trustworthy. It fires for /every/ action Nexus
reaches, at any expansion depth — a server effect returned from inside a client
expansion three levels down is caught exactly like one written at the top of the
vector. The boundary is not a filter over the dispatch you authored; it is a rule
applied to every action the dispatch produces. And the action is interpolated
against the client view before it's set aside, so a client-stage value resolves
before it crosses while a server-stage placeholder crosses untouched.

** Across the wire, and back

The server-bound remainder is transit-encoded and handed back to the Datastar
expression, which =@get=s it to =/offworld-dispatch=. Nexus drains it against the
system atom. A watch on that atom re-renders the entire UI, runs =replicant->d*=
over it, renders to a string, and pushes it down the SSE connection as a
=datastar-patch-elements= event. Datastar morphs the DOM.

** The point

One vector, one authoring site, six stages, four of them in different runtimes,
and the boundary between them marked by a single piece of metadata on a handler —
not by a rewrite, not by a second language, not by a naming convention.

In CSR mode the same code runs with no split: the client view isn't applied, the
interceptor isn't present, and every action runs where it was dispatched. The
split is a runtime decision, not an authoring one.

* The vocabulary

Five concepts. That's the whole surface.

** Intents

A vector of action-vectors, under =:on= in hiccup, keyed by DOM event name.
Life-cycle hooks take the same shape, and Datastar modifiers ride as metadata so
you never hand-roll a debounce:

#+begin_src clojure
{:on {:change [[::toggle [:event.target/checked]]]}}

{:replicant/on-mount [[::init id {:center center}]]}

{:on {:scroll ^{:datastar/modifiers [:throttle.100ms]}
              [[::ng/scroll path
                [:event.target/scroll-top]
                [:event.target/scroll-left]]]}}
#+end_src

** Expansions

A handler that receives state and arguments and returns /more actions/. Where
decisions live.

#+begin_src clojure
(nxr/register-action! ::ng/scroll ^::🪐/server
  (fn [_ path top left]
    [[:effects/save (concat path [:scroll-top]) top]
     [:effects/save (concat path [:scroll-left]) left]]))
#+end_src

(=register-expansion!= is Nexus's newer name for the same call. "Action" is the
data; "expansion" is the handler that turns one into more.)

Expansion recurs, and each action is interpolated just before it expands. So a
handler always receives interpolated values and can branch on them with plain
Clojure — you almost never need to express control flow as data rather than as
code.

** Effects

The drain. An effect is a leaf: it mutates something and returns nothing useful.

#+begin_src clojure
{:node/focus            (fn [ctx _ node] (.focus node))
 :event/prevent-default #(.preventDefault (get-evt %))
 :effects/save          ^:nexus/batch ^::🪐/server
                        (fn [_ system path-vs]
                          (swap! system (fn [state]
                                          (reduce (fn [acc [path v]] (assoc-in acc path v))
                                                  state path-vs))))}
#+end_src

A rule that keeps the registry small: /if an argument decides where to write,
that's not a new effect./ It's =:effects/save= with a different path. Registering
=:set-scroll-top= beside it is a smell.

** Placeholders

A named value that doesn't exist yet. Interpolation replaces the vector with the
value at every action node on the way down, so a placeholder that survives one
expansion still resolves before the effect runs.

#+begin_src clojure
{:event.target/value  #(some-> % get-evt .-target .-value)
 :event/key-modifiers (fn [dd]
                        (let [e (:replicant/dom-event dd)]
                          [(when (.-shiftKey e) :shift)
                           (when (.-altKey e)   :alt)
                           (when (.-ctrlKey e)  :ctrl)]))}
#+end_src

Prefer event-relative placeholders to global lookups. =:event.target/scroll-top=
reads the triggering event's own target; a placeholder doing
=(js/document.getElementById "some-hardcoded-id")= is correct exactly once, and
wrong the moment the component renders twice.

** The side tag

The only genuinely new idea, and it's one keyword in metadata. =^::🪐/server=
means "this handler needs the server". Unmarked means client. In CSR mode the
distinction is ignored entirely.

#+begin_src clojure
(nxr/register-action! ::scan       ^::🪐/client (fn [_ plate] ...))
(nxr/register-action! ::add-filter ^::🪐/server (fn [state path v] ...))

(nextjournal.offworld.nexus.registry/register-many!
 {:nexus/system->state deref
  :nexus/effects       {...}
  :nexus/placeholders  {...}})
#+end_src

* Staging

** The law

#+begin_quote
Computation authored at stage N may *consume* only values already resolved by some
stage ≤ N. It may *carry* — nest, restructure, pass through — a reference to a
later-stage value as opaque data, but never compute with its contents before its
stage arrives.
#+end_quote

The =randomize-button= above obeys this without saying so: =(get-season stem)= is
consumed at render because the server has it; =[:event/key-modifiers]= is only
carried, because nothing at render time can know it.

The law also says when to reach for a data-level conditional, and the answer is
"almost never". Since expansion recurs, a handler can branch with =if= over values
that are already interpolated. The one case the law forces is:

#+begin_quote
The client must decide whether to go to the server *at all*, using a value only
available at client-dispatch time.
#+end_quote

Everywhere else the branch and the value it tests are at the same stage. Here they
aren't: the value appears in the browser and the code that would branch on it runs
on the server, after the round trip you were trying to avoid. Concretely, a
keydown handler that should only commit on =Enter=: with no client-side branch,
every keystroke is a request the server answers by doing nothing.

Sending everything and letting the server decide is always /correct/ — it just
costs the trip. So the guard exists for exactly one purpose and should never grow
into a general control-flow construct. There is no =:and=, no =:or=, no =:cond=;
one guard, deliberately too weak to be tempting, because everything a richer
version could express is already expressible in Clojure at a stage that has the
values.

** Early or late, per value

The law's other half: /each value in an intent picks its own stage/, and the
choice is made by how you write the argument, not by a setting.

=(get-season stem)= is early — it runs once, at render, and the result is baked
into the markup. The clearest case is a counter. =[[:effects/save path (inc n)]]=
freezes a number at render; an expansion of your own — =[[::bump path]]= — names
an operation that recomputes at dispatch against whatever the state has become.
Ten clicks faster than the server can re-render land on =1= in the first spelling
and =10= in the second. Same button, same registry; the difference is entirely
what the vector carries.

Neither is right in the abstract. The question at the call site is whether the
argument stands for /the current value of something/ or is a stable identifier — a
path, a row id, a mode name — that means the same thing whenever it is read.
Stable identifiers are always safe early. It's the "value I read a moment ago"
arguments that freeze whether you meant them to or not. Mostly this doesn't bite,
because the render loop re-takes the snapshot faster than a person can click
again. It bites when something on the render path lags the next click, when the
render loop is deliberately capped below click speed, and when the dispatch source
isn't a person at all — a drag handler, key repeat, a script.

The harder case is an early value too large to serialize into an attribute. Four
ways up, most legible first: *inline it*; *carry a coordinate into immutable
history* — Datomic's =basis-t= — and re-read =as-of= it at dispatch, retaining
nothing; *stash it server-side and carry a claim-check token*; or *hand over the
closure*, everything pinned and nothing legible. Offworld ships the first, because
it's ordinary Clojure, and the fourth, as the =inline= escape hatch. The middle
two are design, not code here, and the second only exists if your data layer can
name its own past — which is the untested claim underneath the whole ladder, that
the snapshot machinery a UI framework must invent is inversely proportional to how
addressable its data layer is.

One consequence for replay cuts the other way: a late-bound read resolves against
/current/ state, so replaying a logged intent re-executes it rather than
reconstructing what the render saw. Binding late reads to a render-time basis
would give both fidelity and durability. Not built.

** What a machine can check

=nextjournal.offworld.staging= makes the ladder explicit:

#+begin_src clojure
[:render :client/state :client/expand :client/fx :server/state :server/expand :server/fx]
#+end_src

Every registration has a /deadline/ — the stage past which an unresolved reference
is stranded. Actions must expand at their side's expand stage, effects run at
their side's fx stage, placeholders get until just before fx.

Not everything is decidable. Whether a handler /consumes/ or /carries/ a value
lives in its body, so a later-stage reference inside an earlier action might be a
perfectly legal carry, and Offworld doesn't guess. What /is/ decidable is the
backward case, where a carry has nowhere to go: =:stranded-client-ref=, a
client-stage reference that survived into the server-bound payload, whose resolver
context is gone; and =:unregistered-action=, a dispatched head resolving to no
handler, which would otherwise be a silent no-op.

#+begin_src clojure
(staging/warn-on!)   ;; divert warns on every dispatch
(staging/report)     ;; grouped summary of everything seen this session
#+end_src

The analysis functions are pure — =lookup=, =tag=, =refs=, =stranded-at-server=,
=unregistered-actions= — so you can run them over a dispatch in a test or a REPL
with no browser.

This works because a dispatch is a tree of keyword-headed vectors and every head
is a key in a registry whose entry carries the side tag. The whole analysis is a
=tree-seq=, a =get-in= and a =meta=; it never runs a handler and never reads one.
Hand it a closure instead and there's nothing to walk — the only way to find out
what a body needs is to execute it.


* State: the stem

Two familiar options, both real. *Prop drilling* gives honest signatures — the
arguments /are/ the scope — but changing the UI tree means re-drilling everything,
and every render-fn invents its own convention for where state lives. *Global
state with a query registry* is concise, and most of the Clojure community has
settled there, but now a render-fn's real dependencies aren't in its signature:
what breaks if I delete this, what is =::macguffin=, why does removing this
component break it? re-frame answers with tooling, and re-frame-10x is a visual
REPL for exactly this problem.

With top-down rendering you can have both, because the global state can simply
/be/ one of the arguments. Immutable data makes this free — you're passing a
pointer, not copying a world.

That's the *stem*: the whole state carried down as a value, alongside a *path*
saying where /this/ render-fn lives inside it.

#+begin_src clojure
(defn main-view [state]
  (for [k [:dresden :hanover]]
    (station-panel (🌿/+ state [:stations k] {:station k}))))
#+end_src

=🌿/+= extends the path and merges in configuration; =🌿/>= replaces the path;
=🌿/local= reads this component's own subtree back out. The stem rides along
untouched. Three concerns stay apart by convention:

- *Configuration* — plain keys. The caller owns the value, its locality, its change.
- *Local state* — reached through =🌿/local=, written at =🌿/path=. /This/ function owns it.
- *Domain* — read out of the stem with an ordinary getter. UI should model neither
  its locality nor its change; that's what puts you in the tar pit.

#+begin_src clojure
(defn get-season [stem] (get-in stem [::path :to :season] :spring))

(defn get-day {::🌿/deps #{`get-season}} [stem]
  (season->holiday (get-season stem)))
#+end_src

Getters are just functions of the stem. =::🌿/deps= is a seed for dependency
tracing — =🌿/q= and =🌿/trace= record a call tree at runtime — but the static
registry it's aiming at isn't finished. Treat =deps= as documentation today.

** A path is an id

=🌿/id= is a pure derivation of =🌿/path=, so an element's name comes from its
position in the /render/ composition — not from a mount-time counter, an insertion
order, or a place in the DOM. That's what makes it survive morphing: Datastar
replaces nodes underneath you, but it cannot perturb a name the markup itself
derives: the name is a function of what the thing /is/ in the composition, not of
when or where it appeared.

The =::🌿/el= placeholder closes the loop, resolving a path or id to a live node
at client-dispatch time:

#+begin_src clojure
[[:node/hide-popover [::🌿/el popover-id]]
 [:node/focus        [::🌿/el choice-id]]]
#+end_src

To build a persistent "component" with JavaScript batteries included, all you need
is a path and an id. That sounds like two things. It's one.

* Talking to the browser

** Life-cycle without a component model

Replicant hands the DOM node to placeholder- and action-handlers, and that's
enough. No refs, no hooks, no =componentDidMount=.

The demo's map widget is the whole pattern in twenty lines. On mount, a client
effect constructs a MapLibre object and calls the injected =remember= fn, which
stores it in a =WeakMap= keyed by the node. Later, another element's handler names
the node's id and =🪐/recall= gets the object back.

#+begin_src clojure
[:div {:id id
       :data-ignore-morph true
       :replicant/on-mount [[::init id {:center center}]]}]

[:button {:on {:click [[::pan-to id 4.89 52.38]
                       [:effects/save path {:center [4.89 52.38]}]]}}
 "Amsterdam"]
#+end_src

The =WeakMap= subordinates the JS object's lifetime to the node's. Unmount the div
and the map is garbage; mount it again and =on-mount= builds a fresh one, seeded
from the state that same click already saved. Persistence across mounts — and
across sessions, in SSR — falls out of saving the state and reinitializing from
it. Because the pattern is portable data it is identical in all three modes:
Datastar's morph does the mounting in SSR, Replicant's reconciler in CSR, and
neither the render-fn nor the handler can tell.

** Why we don't bubble

The DOM event system fuses three unrelated jobs, and only one is contentious.
*Delegation* — one listener serving many elements — is pure mechanism.
*Default-action negotiation* — submit, navigation, checkbox toggle — is the one
channel the browser offers for its own participation. *Announcement* — an inner
element throws a =CustomEvent= and whichever ancestors happen to contain it may
act — is the architecture, and Offworld declines it.

*The usual reason to delegate doesn't apply.* You delegate to a stable ancestor
because the server swaps in new HTML and your imperatively-attached listeners are
gone. We never attach imperatively: the listener is an attribute in the rendered
markup, so its call site ships with the DOM on every morph.

*The cost of announcement is an obligation, not a name.* Salesforce's guidance is
blunt: "When an event bubbles, it becomes part of your component's API and every
consumer along the event's path must understand the event." Namespaced keywords
dispose of the collision half of that complaint. The obligation half survives
renaming, because it was never about names. And =stopPropagation= makes it worse
by handing a leaf ambient authority — it silences handlers it has never heard of,
analytics and focus management and a parent's keyboard router alike. And the event
buys less in exchange than it looks: =dispatchEvent= runs its listeners on the
current call stack, so against a direct call it buys late binding, zero-or-many
callees and error isolation — name-decoupling, never time-decoupling.

*Bubbling routes by containment; injection routes by naming.* Containment routing
makes the topology of your markup part of your control-flow graph: wrap something
in a div, move a cell into a sticky header, and you have silently changed who
hears an intent. React conceded this the moment physical placement diverged from
logical composition — "Events from portals propagate according to the React tree
rather than the DOM tree" — building a second propagation path that follows
composition instead of containment.

*So what do we do instead?* We pass the ids in, and the intent names its
recipients. Here is the omnibox handling =Enter= on a choice item — one handler
acting on three named elements plus the event itself, all spelled at the site that
decides:

#+begin_src clojure
"Enter" [[:event/prevent-default]
         [:node/hide-popover [::🌿/el popover-id]]
         [:node/blur         [::🌿/el anchor-id]]
         [:node/set-checked  [::🌿/el choice-id] true]
         [:effects/conj (conj path :filters) (first filters-to-add) #{}]]
#+end_src

This is exactly what a component framework would express by bubbling. Here the
render code says what the keypress /means/, nothing along any path is obliged to
know, and =[:event/prevent-default]= sits in the same vector — default-action
negotiation is reachable from data, so we never needed the event as an
architecture, only as a transport. The failure modes differ too: an injected id
can be missing and =::🌿/el= resolves to nothing, where a bubbled event with no
listener is indistinguishable from one that worked.

*When does bubbling still win?* Not on subscriber count — any consumer set
derivable at render time is injectable, and under =view = f(state)= that's all of
them. The real discriminator is *authorship*:

#+begin_quote
Bubbling is the interop protocol with code that isn't in your render tree. Inside
your render tree it buys nothing that injection doesn't.
#+end_quote

Which leaves DOM you didn't render, consumers arriving without a re-render, and
the browser as the consumer of a default action — plus one fair concession. A
single gesture can reasonably fire two differently-named events: a per-frame one
that never leaves the client, and one on release that commits. For the per-frame
leg a public DOM name is right, because the widget is /offering/ something any
container may subscribe to rather than addressing a recipient it could have named.
An ancestor can observe, cancel or re-emit it, though never restructure what was
/meant/ — which is what a middle layer does to an intent that arrives as data.
Delegation likewise stays available as an optimization: one ancestor listener
reading =.target=, in place of N per-element handlers, costs literacy and buys
markup. Per element by default; delegate on measured cost.

** Frame-perfect work

#+begin_quote
An intent is one per decision, not one per sample.
#+end_quote

Nexus should start and stop a gesture. It should never see a =pointermove=. The
per-frame work — the drag preview, the pointer capture, the style writes — belongs
inside a client effect owning its own rAF loop, torn down by a second intent. If
you're dispatching per frame, the granularity is wrong.

=preventDefault= must be called while propagation is happening, so the
browser-as-participant is reachable only at client-dispatch time and only while
that stage stays synchronous. But interpolation reads the event's values first, so
the rest of a dispatch /could/ be deferred safely — we hold a snapshot, not a live
event whose =target= the platform may still rewrite. Cancellation is the one thing
that can't be deferred.

And one thing the pipeline doesn't settle. Between the commit and the morph that
confirms it, something has to /own/ the property the gesture was changing — a
column width, say — and keep re-asserting it across any morph landing in the gap.
Offworld has places to hold a client-owned value (=data-ignore-morph=, Replicant's
per-node memory) and no vocabulary at all for the /handoff/: when the client may
stop asserting. That's hand-rolled per gesture today, and it's the one part of a
rich seam this design doesn't make legible.


* Three modes

The same expression, three runtimes. This is what the library is named after.

** SSR

The browser holds no state. This is the mode walked through above: =divert=
splits, the server drains its half into an atom, a watch re-renders the whole UI
and pushes it down an SSE connection, and Datastar morphs.

#+begin_src clojure
(add-watch system ::ui/render
  (fn [_ _ _ new-state]
    (broadcast-elements!
     (sse-message {:event "datastar-patch-elements"
                   :lines [["elements" (-> new-state 🌿/init-state ui/render
                                           🪐/replicant->d* rstr/render)]]}))))
#+end_src

Yes — that re-renders everything, every time, and sends it over the wire. Brotli
and morph diffing make it cheaper than it sounds, and it buys one state in one
place with the DOM a pure function of it. Committing to top-down rendering is the
trade.

Note what the library is /not/ deciding there. Rendering on every change is a
watch the demo's server installs; a loop ticking on its own interval and pushing
only when the render differs is the same contract on a different schedule.
Offworld's side is intents in by request, HTML out — so a burst of eight
dispatches costs eight pushes under the watch and one under a capped loop, without
a line of view code changing. Scheduling changes how often the browser is told,
never what is true.

** CSR

Replicant's dispatch goes straight to Nexus against a local atom. No client view
is applied and no diverting interceptor is installed, so nothing splits and no
payload is ever encoded.

#+begin_src clojure
(r/set-dispatch!
 (fn [dispatch-data actions]
   (if js/navigator.onLine
     (nxr/dispatch system dispatch-data actions)
     (🌠/offline-dispatch dispatch-data actions))))
#+end_src

The intended arc is that the SSR page paints from server HTML with no JavaScript
on the critical path, prefetches this bundle, and hands off in the background so
the app becomes a rich client afterwards. The prefetch and the handoff hook are
wired (release builds only). The last step isn't: the handed-off bundle registers
its dispatch fn but never takes over rendering, so today =?csr= is how you
actually get the client-rendered app.

** Offline

This is where representing intents as data stops being an aesthetic preference.

An SSR page that loses its connection is normally dead — its state lives on a
server it can't reach. Offworld's answer is to have the server /leave a snapshot
behind/. =offline-capable= wraps a subtree in a div, encodes the paths that
subtree actually needs, and parks them in a =data-offworld-sync= attribute:

#+begin_src clojure
(🌠/offline-capable
 {:id           "scan-game-offline"
  :render-fn    #'scan/offline-game
  :select-paths #{[::scan/scans] [::scan/plates]}
  ::🌿/path      [:scan-game]
  ::🌿/stem      stem}
 (scan/game (🌿/+ state [:scan-game])))
#+end_src

On the =offline= event, =go-offline!= collects every such node, rebuilds a stem
from the parked paths, flips the ux to =:csr=, and re-renders those subtrees with
Replicant — in the browser, from the same render-fns, with no server. Dispatches
keep working: server-bound actions can't run, so =offline-dispatch= appends them
to an action log and runs everything else locally.

A log of intents is replayable because an intent is a value. That's the whole
argument for data, cashed out — and the going-offline half works. The demo makes
the divergence visible on purpose, each truck showing a "Client" status and a
"Server" status that disagree while you're disconnected.

The coming-back half is not finished. =go-online!= is meant to ship the log to
=/offworld-go-online= and have the server drain it as though it had arrived all
along. What it does today is interpolate the log and the offline stem into a query
string un-encoded, into a handler whose decode step is commented out. The shape is
right; the leg does not work. Treat offline as a demonstration that the
representation supports replay, not as a reconnect story you can ship.

* Ordering

Dispatches race. A throttled scroll handler can put three actions on the wire and
have them arrive out of order, and =assoc-in= is not commutative when the same
path is written twice.

=nextjournal.offworld.order= holds policies as metadata on the dispatch.
=:seq-gate= stamps a sequence number per key and the server drops anything that
isn't next — correct for "latest wins" streams like scroll position.
=:bounded-buffer= holds out-of-order actions in a small buffer and flushes on
contiguity, overflow, or timeout — correct when you need every action, in order.

Ordering is a policy, not part of the client/server boundary, so it's two
interceptors of its own rather than a step buried in the split. Nothing in this
library installs either:

#+begin_src clojure
(update nexus :nexus/interceptors conj (📈/proposing !counters))    ; client
(update nexus :nexus/interceptors conj (📈/checking  !actor-state)) ; server
#+end_src

=proposing= runs =:after-dispatch= and stamps whatever is on its way to the
server. =checking= runs =:before-dispatch= and applies the policy before anything
runs. A dispatch carrying no policy passes through both untouched, and a dispatch
that skips ahead when nobody is checking simply runs — the honest shape of an
opt-in guarantee.

Nothing runs unless a policy asks for it: a =:drop= says so outright, and a bare
=:timeout= means /held, not yet/, so a buffered gap waits rather than executing
while it waits for its predecessor. Scheduling stays yours — =checking= takes an
=:on-timeout= callback and hands the request over rather than reaching for a timer
the library has no business owning; call =handle-timeout= when it fires.

Both policies and both interceptors are implemented and tested. What has no worked
example yet is a UI that actually needs one, so treat the policy set as two
plausible answers rather than two proven ones.

* Working in it

*No treasure hunt.* To find out what a click does in an event-driven UI you go
looking: the listener might be on the element or delegated three files away,
something on the path might call =stopPropagation=, a shadow boundary might mean
the =target= you'd inspect isn't the one that fired. The wiring is only complete
at runtime, in the live DOM — which makes the DOM the document. Here the answer is
the form you're already looking at: =randomize-button= states its whole story
where it's written, no ancestor will intercept it, nothing elsewhere has quietly
subscribed. That runs the other way too — delete a render-fn and nothing silently
loses a listener; move a subtree into a wrapper div and no behaviour changes.

*Everything is greppable, exactly.* Every name is a namespaced Clojure keyword,
with one definition site and a closed set of use sites. =rg '::randomize'= finds
the registration and every dispatch, no false positives. Compare a =CustomEvent=
called ="columnresize"=: a string in two unrelated files, no definition site,
nothing distinguishing it from the same word in a comment. Miss a site while
renaming and it surfaces as an =:unregistered-action= naming the key, not as a
listener that quietly stops firing.

*The REPL is the whole harness.* An intent is a value your render expression
/returns/, so you can look at it — an inline =def=, or hashp's =#p=, anywhere in
the composition chain:

#+begin_src clojure
[:button {:on {:click #p [[::randomize
                           [:event/key-modifiers]
                           (get-season stem)]]}}
 "Randomize (shift-click to reset)"]

;; => [[:nextjournal.offworld.demo.ui.holiday/randomize
;;      [:event/key-modifiers]
;;      :spring]]
#+end_src

What prints is the finished staged computation /at that call site/: render-stage
values collapsed, later-stage names still standing as data. Everything the outer
callers injected is in there too, because injection is function application — the
composition is a value at every rung, not just the top. Capture two across a state
change and =clojure.data/diff= them.

There is no equivalent when behaviour is /announced/ rather than composed: a
bubbling event has no value to look at, and nothing exists until a real gesture at
runtime.

The rest follows: a dispatch is a value, so you can build one and run the
machinery over it with no browser and no server. Render-fns likewise — the
arguments /are/ the scope, so calling one is calling a function. No mounting, no
test renderer, no jsdom.

#+begin_src clojure
(nexus/expand-actions (nxr/get-registry) nil actions dispatch-data)
(staging/unregistered-actions (nxr/get-registry) dispatch)
(station-panel (🌿/+ state [:stations :dresden] {:station :dresden}))
#+end_src

*What this gives an agent.* Locality, greppability and a REPL-runnable dispatch
matter more for a coding agent than for a person, because an agent has less
context and no eyes. The unit of change is one form in one file, and editing an
intent needs no model of the runtime DOM because the DOM never held the wiring.
Verification needs no browser: build a dispatch, expand it, assert on the result,
or run =staging/report= — where most UI work is otherwise unverifiable except by
screenshot. Mistakes are named rather than reproduced. And the vocabulary is a map
you can print: =(nxr/get-registry)= is the whole contract.

* What it costs

*A call site doesn't show its staging.* Reading =[[:effects/save path v] [::pan-to
id 4.89 52.38]]=, nothing tells you the first runs on the server and the second in
the browser. It's legible as data and illegible as /staging/. Nothing is actually
hidden — the registry declares side and shape for every key — so this is a tooling
gap, and largely a closed one: the *divert atlas* statically scans every
registration site and classifies each key by side and kind, and
=offworld-atlas-mode= font-locks that into any Clojure buffer (earth green for
server, off-world violet for client, texture for effect / expansion /
placeholder), with the reading on hover and at point via eldoc. The same
classifier builds a synthetic registry and runs the staging checks without loading
or executing anything. Caveats, none fatal: the classification is /per key/, so it
says what =:effects/save= always is rather than whether this dispatch is
well-formed; the analysis is textual rather than macroexpanded; and the faces
table is a generated snapshot that drifts unless regenerated.

*Placeholders are an ambient rule, and an unresolved one is invisible.* The
sharpest edge in the design. Interpolation walks the entire action tree, children
first, recognising a placeholder by looking a vector's head up in the registry —
at any depth, in any argument position, map keys included. So a placeholder that
fails to resolve, through a typo or a stale client bundle or a registration that
never ran, is indistinguishable from a vector you meant to write. It travels on as
data and nothing reports it. The tell is specific: a value arrives server-side as
=["~:event.target/scroll-top"]= instead of a number while the rendered HTML looks
perfectly correct. The reverse direction is live too — interpolation runs again on
the server against the /server's/ registry, so a keyword-headed literal you
authored can be captured by a placeholder your code never heard of.

The checker can't help, and =staging.cljc= admits why: =keyword-headed?= is "the
shape of both a dispatched action and a reference". Head position is discriminated
by structure and is checkable; argument position is discriminated by registry
membership, which is global, load-order dependent, and may differ between the two
runtimes. The honest fix isn't a lint but a distinguishable /value/ — a tagged
literal or a record — so "is this a placeholder?" is a property of the value
rather than of whatever happens to be in a registry. Unresolved would become an
error, and authored data could never be captured. Not done.

*A silent no-op is possible.* An unregistered action does nothing and says nothing
unless =warn-on!= is on. Turn it on in dev.

*Stack traces point at the loop.* An effect that throws traces through Nexus's
dispatch loop, not the render-fn that composed the intent. Nexus's action log
(=nexus.action-log/create-log=, with a Dataspex panel) recovers most of what the
trace doesn't tell you, but the trace is still one indirection removed.

*Explicit state is still explicit.* The stem makes threading cheap, but a
render-fn needing three things still takes three things. That's the trade for
signatures that tell the truth, and it is a trade.

*And underneath all of them: this is an execution model.* Any library that decides
how an interaction is sequenced carries a risk its authors rarely spell out — a
lesson re-frame learned about its own machinery over a decade, and one Offworld
inherits by being downstream of it. The mitigation is deliberate: keep the
machinery small enough to read end to end (three kinds of registration, one loop,
one metadata tag), and make what it decides inspectable rather than ambient.
Placeholder interpolation is the one rule that currently fails that second test.

* Objections

*Isn't this Greenspunning?* Partly, and unavoidably. The question isn't whether to
build an abstract machine but whether to admit you have one. The charge worth
taking seriously is turning control flow into data, and the staging law answers
it by
narrowing that case to one: eliding a round trip.

*Why not infer the boundary with macros?* Hyper and Electric Clojure do versions
of this and buy real ergonomics. What they don't leave you with is a value — so
nothing to walk, check, colour or log, and a behaviour change that shows up in
review as an altered string.

*Isn't shipping data slower than shipping code?* Two costs get confused. Measured
on a 10,000-row table: on the /wire/, compression handles it — the markup came to
around 200KB Brotli'd, and replacing each cell's inline intent with a shared
template or an opaque server-held token bought about 12% more. On /parse/, neither
move helped at all, because the browser decompresses back to full text and parse
time tracks raw bytes. The opaque-reference alternative is the smaller one, not
the faster one. And the data form is a source, not an obligation: a
statically-known dispatch could in principle compile to direct client code, at the
cost of two implementations that must be proven to agree. Not built.

*Why not signals?* A signal is a defunctionalized /setter on a mutable place/ —
stringly-typed, its meaning in a callback rather than on the wire. Fine for
residual state that wants a cell (a column width, =<details open>=); poor for
intent, which wants to be logged, replayed and carried across a boundary. Offworld
uses neither: residual presentation state goes on the DOM node or in Replicant's
per-node memory. Read the critique narrowly, though — "not data" is not the same
as "a place", and a client-side capability addressed by an id is neither a mutable
cell nor a setter. What the critique lands on is making the place-cell your
general model of client state.

*Why not cursors?* A cursor fuses read and write onto one path, and Offworld
already hands a render-fn both halves separately — =🌿/local= reads at the path,
=[:effects/save path v]= writes at it. Why not fuse them?

Because that defunctionalizes the /place/ rather than the /intent/, and the two
aren't recoverable from each other. From =[:effects/save [:panel :season] :winter]=
you can't tell someone shift-clicked Randomize; from =[::randomize [:shift]
:spring]= you can recover both what happened and, by running the expansion, the
write it produces. Which decides three things once a write has to leave the
process: you can ask whether this user may randomize the season, but not
meaningfully whether they may set a path to a value; replaying places overwrites
where replaying intents re-decides; and a middle layer can restructure an intent
it doesn't understand, where a write it can only intercept. There's a mechanical
reason too — a cursor is a closure over a path, so it can't cross the wire, and
defunctionalizing it gives you back =[:effects/save path v]=. A cursor is what you
have /before/ the boundary forces your hand.

We keep the addressing half: =🌿/+= extends a path, =🌿/local= reads at it — the
read end of a cursor with no write end attached. Reading at a path was never the problem — which makes the
stem itself fair to point at, since =🌿/local= plus =[:effects/save path v]= /is/
a cursor with the halves left apart. Offworld reaches for that deliberately for a
component's own local state, where the state tree is isomorphic with the render
call tree and addressing by path adds no coupling the composition didn't already
have. The line is about the place model /globally/, for shared and domain state,
and then discovering every component knows the shape of the whole store. re-frame
put it more bluntly a decade ago: "Please, just say no. We already know where that
goes. As your programs get bigger, the use of these two-way constructs will
encourage control logic into all the wrong places and you'll end up with a
tire-fire of an Architecture."

The usual framing of that argument — command-query separation — is the weaker
version: a strictly one-way system dispatching =[:set-path [:a :b] 1]= everywhere
has obeyed it and gained nothing. The problem isn't that reads and writes travel
together; it's that the write names a location instead of a meaning.

And the concession: for genuinely residual state — is this disclosure open, what
is half-typed in this box — the intent /is/ the place-write, and "you made me
register an action to toggle a boolean" is fair. The reason to hold the line is
that whether a piece of state is residual turns out to be knowable per
application, not per widget.

*Do I have to register everything?* For a one-off server operation, no. The
=inline= macro registers a closure at render time against the current connection
and emits =[[::inline/invoke "<token>"]]=; the server looks the token up and calls
it, and the table is dropped when the connection closes.

It costs every property listed under [[*A function can't cross the wire][A function can't cross the wire]]: the intent on the
wire is a UUID, so there's nothing to check, colour, authorize by shape, or replay
— the token dies with the connection. It mints a fresh token per render, so an
attribute a stable key would leave alone churns on each morph. And it runs only on
the server, so it's no answer to the residual-/client/-state complaint above. Two
escape hatches, two different complaints, neither covering the other.

*Where's the component model?* There isn't one, and the demo does without: a path
for identity, an id for addressing, a =WeakMap= for lifetime, life-cycle hooks
dispatching ordinary actions. Adding one would mostly mean rebuilding what the DOM
already does.

*Is this the right bet for my app?* Often not. If your app is connected-only and
CRUD-shaped, the simplest thing that could work is one global default — every
interaction an opaque server closure behind a minted key, no registry, no side
tag, no staging vocabulary. Designs of exactly that shape run real production apps
today; this one does not, and saying otherwise would be selling.

Two things survive that concession, on different clocks. The first is narrower
than the pitch and more solid: even in an app that never renders a frame in the
browser, one data DSL spanning client and server state at a single authoring site
is an ergonomic win, and what it displaces isn't a rival framework — it's a
stringly-typed attribute cell plus a bundle of JavaScript to give it meaning. The
second is the hybrid dividend — offline, handover, a client-side branch that saves
a round trip — and that one is a hypothesis. The properties described here are
real and checkable; that they add up to advantages still needs proving, and the
burden is on this design, not the simpler one.


* Status

This is a research library, not a product.

- *Solid:* the dispatch split, placeholders, the side tag, SSR and CSR modes, the
  stem, the staging analysis, =inline=.
- *Implemented and tested, no worked example:* both ordering policies and the two
  interceptors that apply them. Exercised by tests, not by a UI that races.
- *Half working:* offline mode. Going offline works — subtrees re-render locally
  from the parked snapshot and dispatches keep running. Coming back doesn't: the
  action log ships un-encoded and the receiving handler's decode is commented out.
  =select-paths= is a blunt API and =cache-queries= is collected but never read.
  The CSR handoff is in the same state: prefetch and hook wired, takeover not.
- *Seeded, incomplete:* dependency tracing (=🌿/q=, =🌿/trace=, =::🌿/deps=) — the
  runtime trace works, the static registry behind =defc=/=defq= isn't wired.
- *Built, but in a different repo:* the divert atlas and =offworld-atlas-mode=. The
  classifier reads Offworld's =staging= namespace directly and arguably belongs
  here. It's an editing aid, not a gate — nothing in CI re-emits the faces table or
  fails on a staging violation.
- *Missing, and wanted:* a distinguishable representation for placeholders, so an
  unresolved one fails loudly instead of passing as data.
- *Speculative:* compiling statically-known dispatches to direct client code.

* Glossary

Terms this document uses, in the sense it uses them.

*From the three libraries underneath*

- hiccup :: Clojure vectors as markup — =[:button {:class "x"} "Click"]=. Replicant renders it.
- morph :: patching an existing DOM tree in place to match a new HTML fragment, keeping the nodes that didn't change. Datastar's alternative to replacing a subtree.
- SSE :: server-sent events. One long-lived HTTP response the server keeps writing to, so it can push without being asked.
- action :: one =[:keyword arg arg]= vector. Nexus's unit of work.
- dispatch :: handing Nexus a vector of actions and letting it drain them.
- expansion :: a handler that receives an action and returns /more actions/. Where decisions live. (Nexus also calls these action handlers.)
- effect :: a handler at the end of the chain. It mutates something and returns nothing useful.
- placeholder :: a vector standing for a value that doesn't exist yet, like =[:event.target/value]=.
- interpolation :: walking an action tree and replacing every placeholder with the value it stands for.
- interceptor :: a function Nexus calls at a named point in its loop — before a dispatch, before each action, after each effect — which can inspect or rewrite what's passing through.

*Offworld's own vocabulary*

- intent :: a whole =:on= vector: the actions one user gesture means. The unit you author.
- stage :: one link in the fixed chain a hybrid interaction passes through — render, then client state, expansion and effects, then the same three on the server.
- staged computation :: an interaction written as one value whose parts are evaluated at different stages, rather than as code that runs all at once.
- the side tag :: =^::🪐/server= or =^::🪐/client= metadata on a handler, saying which machine it needs.
- divert :: the client-side split. It runs the client half of a dispatch locally and hands the server half back to Datastar.
- stem :: the whole application state, passed down to a render-fn as an argument, alongside a path saying where that render-fn lives inside it.
- SSR / CSR :: server-side and client-side rendering. Offworld runs the same code in both, plus an offline third mode.

*Terms borrowed from elsewhere*

- closure :: a function together with the bindings it captured where it was written. The captured half is why one can't be sent over a wire.
- defunctionalization :: replacing a function value with a name plus the values it captured, and one dispatcher that applies the name to them. Reynolds, 1972.
- ambient authority :: the power to affect something you never named and can't see. =stopPropagation= is the example here: a leaf silences listeners it has never heard of.
- residual state :: the small presentational facts a UI accumulates that nothing else needs — is this disclosure open, what is half-typed in this box.
- cursor :: a read/write handle onto one path inside a larger value, so the holder can get and set without knowing where it is.
- signal :: a mutable cell that notifies dependents when it changes. The reactive-graph primitive Offworld doesn't use.
- prop drilling :: passing state down through every intermediate call, so a render-fn's arguments are its whole scope.
- Greenspun's tenth rule :: the joke that every large program contains an ad-hoc, informally-specified implementation of half of Lisp. Invoked whenever someone builds an interpreter without meaning to.

* Sources

- [[https://replicant.fun/][Replicant]] — rendering, life-cycle hooks, [[https://replicant.fun/tutorials/javascript-interop/][JS interop]] and per-node memory
- [[https://github.com/cjohansen/nexus][Nexus]] — actions, effects, placeholders
- [[https://data-star.dev/][Datastar]] — attributes and SSE morphing
- [[https://day8.github.io/re-frame/][re-frame]] — the unidirectional loop this is downstream of, and its own honest
  writing on the cost of an execution model
- [[https://day8.github.io/re-frame/historical/#guiding-philosophy][re-frame: guiding philosophy]] — the primacy of data, and the case against
  read/write cursors and two-way binding
- [[https://developer.salesforce.com/docs/platform/lwc/guide/events-propagation.html][LWC: event propagation]] and [[https://developer.salesforce.com/blogs/2021/08/how-events-bubble-in-lightning-web-components][How events bubble in LWC]] — the clearest statement of
  what bubbling costs a component's API
- [[https://blog.angular-university.io/angular-component-design-how-to-avoid-custom-event-bubbling-and-extraneous-properties-in-the-local-component-tree/][Angular: avoiding custom event bubbling]] — the mirror-image problem, and why a
  framework without cheap threading chooses a different implicit channel
- [[https://dev.to/open-wc/composed-true-considered-harmful-5g59][composed: true considered harmful?]] — Westbrook Johnson, 2019
- [[https://pm.dartus.fr/posts/2021/shadow-dom-and-event-propagation/][Shadow DOM and event propagation]] — retargeting, and =composedPath()= as an
  "escape hatch to the shadow DOM encapsulation model"
- [[https://react.dev/reference/react-dom/createPortal][React: createPortal]] — events propagate by the React tree, not the DOM tree
- [[https://blog.logrocket.com/deep-internals-event-delegation/][Internals of event delegation]]
- [[https://dl.acm.org/doi/10.1145/800194.805852][Reynolds: Definitional Interpreters for Higher-Order Programming Languages]] (1972)
  — where defunctionalization is introduced
- [[https://arxiv.org/pdf/2111.10413][CPS, defunctionalization, accumulations and associativity]] — Danvy et al.
- [[https://www.w3.org/2001/tag/doc/leastPower.html][Berners-Lee & Mendelsohn: the rule of least power]] — why the weaker notation is
  the one you can analyse, which is the rule the staging ladder follows
- [[https://www.itu.dk/people/sestoft/pebook/][Jones, Gomard & Sestoft: Partial Evaluation]] — binding-time analysis, the static /
  dynamic split that [[*Early or late, per value][Early or late, per value]] is an informal version of
- [[https://docs.datomic.com/reference/filters.html][Datomic: database filters]] — =as-of= and =basis-t=, the coordinate-into-history
  that makes rung 2 of that ladder a long instead of a snapshot store
- [[https://www.enterpriseintegrationpatterns.com/patterns/messaging/StoreInLibrary.html][Enterprise Integration Patterns: Claim Check]] — store the payload, pass a token,
  redeem it later; rung 3
